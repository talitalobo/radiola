read.table(file = paste(path.arquivos,arquivo,sep=""), sep="\t", stringsAsFactors = T, header = T)
arquivo
read.table(file = paste(path.arquivos,arquivo,sep=""), sep="\t", stringsAsFactors = T, header = T)
read.table(file = paste(path.arquivos,arquivo,sep=""), sep="\t", stringsAsFactors = T, header = T)
arquivo <- arquivos[iarq]
arquivo
file
paste(path.arquivos,arquivo,sep="")
read.table(file = paste(path.arquivos,arquivo,sep=""), sep="\t", stringsAsFactors = T, header = T)
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
arquivos[iarq]
arquivo <- arquivos[iarq]
read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T)
a<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T)
View(a)
a<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T)
View(a)
a<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T)
View(a)
?read.table
a<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
View(a)
for(iarq in 1:length(arquivos)){
arquivo <- arquivos[iarq]
a<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
arquivo <- arquivos[iarq]
a<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
a<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
View(a)
a<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
for(iarq in 1:length(arquivos)){
arquivo <- arquivos[iarq]
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
arquivo <- arquivos[iarq]
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
arquivo
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
View(b)
paste(path.arquivos,arquivo,sep="")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
b<-read.table(file = paste(path.arquivos,arquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- cbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- c()
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- cbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- c()
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
View(arquivo)
View(a)
View(arquivo)
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- c()
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- cbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- c()
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- c()
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- cbind(arquivo, novoArquivo)
}
novoArquivo <- c("ano", "alltags", "musica", "artista")
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- cbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- c("ano", "alltags", "musica", "artista")
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
View(novoArquivo)
View(novoArquivo)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
View(novoArquivo)
read.table(file = "1966.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
read.table(file = "musicas/1966.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
a<-read.table(file = "musicas/1966.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
View(a)
a<-read.table(file = "musicas/1966.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
a<-read.table(file = "musicas/1966.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
a<-read.table(file = "musicas/1966.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
a<-read.table(file = "musicas/1966.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
a<-read.table(file = "musicas/1966.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
View(novoArquivo)
View(novoArquivo)
a<-read.table(file = "musicas/1972.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
a<-read.table(file = "musicas/1972.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
a<-read.table(file = "musicas/1972.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
#  a<-read.table(file = "musicas/1972.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
View(novoArquivo)
View(novoArquivo)
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
#  a<-read.table(file = "musicas/1972.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
View(a)
path.arquivos <- 'musicas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
#  a<-read.table(file = "musicas/1972.tsv", sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
}
View(novoArquivo)
novoArquivo$alltags <- NULL
path.arquivos <- 'musicasTresColunas/'
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
paste(path.arquivos,nomeArquivo,sep="")
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
View(arquivo)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
nomeArquivo
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
#  novoArquivo <- rbind(arquivo, novoArquivo)
}
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
path.arquivos <- 'musicasQuatroColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
View(novoArquivo)
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo2 <- data.frame(ano=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo2 <- rbind(arquivo, novoArquivo2)
}
path.arquivos <- 'musicasQuatroColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
novoArquivo$alltags <- NULL
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo2 <- data.frame(ano=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo2 <- rbind(arquivo, novoArquivo2)
}
View(novoArquivo2)
path.arquivos <- 'musicasQuatroColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
novoArquivo$alltags <- NULL
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo2 <- data.frame(ano=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo2 <- rbind(arquivo, novoArquivo2)
}
path.arquivos <- 'musicasQuatroColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
novoArquivo$alltags <- NULL
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo2 <- data.frame(ano=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo2 <- rbind(arquivo, novoArquivo2)
}
todasMusicas <- rbind(novoArquivo,novoArquivo2)
View(todasMusicas)
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo2 <- data.frame(ano=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo2 <- rbind(arquivo, novoArquivo2)
}
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo2 <- data.frame(ano=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo2 <- rbind(arquivo, novoArquivo2)
}
path.arquivos <- 'musicasQuatroColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
novoArquivo$alltags <- NULL
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo2 <- data.frame(ano=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo2 <- rbind(arquivo, novoArquivo2)
}
todasMusicas <- rbind(novoArquivo,novoArquivo2)
View(todasMusicas)
path.arquivos <- 'musicasQuatroColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo <- data.frame(ano=numeric(0), alltags=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo <- rbind(arquivo, novoArquivo)
}
novoArquivo$alltags <- NULL
path.arquivos <- 'musicasTresColunas/'
arquivos <- list.files(path.arquivos)
novoArquivo2 <- data.frame(ano=numeric(0), musica=numeric(0), artista=numeric(0))
for(iarq in 1:length(arquivos)){
nomeArquivo <- arquivos[iarq]
arquivo<-read.table(file = paste(path.arquivos,nomeArquivo,sep=""), sep=";", stringsAsFactors = T, header = T, fileEncoding = "UTF-8")
novoArquivo2 <- rbind(arquivo, novoArquivo2)
}
todasMusicas <- rbind(novoArquivo,novoArquivo2)
write.table(todasMusicas, "musicas.csv",sep=";",quote = F,row.names = F)
